using UnityEngine;

public class BuildingViewBasic : MonoBehaviour
{
[Header("Prefab中配置")]
    [SerializeField] protected Transform defaultSpawnPoints;
    [SerializeField] protected Transform defaultLaunchPoint;
[Header("Animation")]
    [SerializeField] private Animator m_animator;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
